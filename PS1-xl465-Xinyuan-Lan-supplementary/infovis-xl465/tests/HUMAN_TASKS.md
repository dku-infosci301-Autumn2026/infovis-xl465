# Student-run interpretation check — not yet performed
Use this only within the course-approved process. Do not collect names, recordings or sensitive data. Confirm voluntary participation; allow stopping. Do not publish confidential peer feedback. Record anonymized aggregate outcomes and limitations.

Ask three to five consenting classmates, clearly labeled as proxies for novice learners, to complete two matched tasks in opposite condition orders. Keep task difficulty comparable and record order. Use a stopwatch only when the person starts; system latency is not task time.

1. Find a component that shows change over time; explain one input or output; locate a related example if available.
2. Find a component that selects records by attributes; explain the difference between its blueprint annotation and a linked example.

Scoring fixed before the session: correct component 1 point; correct input/output explanation 1; understands example is not the original implementation 1. Record elapsed seconds, score 0–3, failure or hesitation, condition order, and voluntary clarity rating 1–5. If an example is unavailable in the baseline, record this as unavailable; do not score its absence as user error. For comprehension comparison use component and input/output points shared across both conditions. Study a screenshot/manual original baseline separately from retrieval ablations; do not conflate them.

Rows to fill: participant code | task | condition/order | seconds | shared score /2 | source/example distinction | observed issue.

Do not enter made-up participants or results. The current paper reports technical fixtures only. After actual testing, update the Findings and Appendix A5, keeping the technical results separate.
