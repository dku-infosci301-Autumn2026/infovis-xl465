# Protocol fixed before the first retrieval run
Date: 2026-09-13. These 12 AI-authored diagnostic fixtures target the six mapped capability families; they are not participant questions or an independent benchmark. Acceptable component IDs were fixed before executing retrieval. Q08 intentionally tests new example vocabulary and is reported separately through raw rows. No tuning on results is planned.

Compare four conditions on the SAME 27 nodes: K0 BM25/source descriptions; K1 BM25/source + linked example descriptions; S0 MiniLM/source; S1 MiniLM/source + linked example descriptions. Keep candidate pool, filtering and result limit fixed. Additional data also expose the linked specification. Count a hit if any acceptable component is in the top 3; report reciprocal rank over the returned top 6. This is technical task coverage, not measured comprehension. Provisional success: S1 reaches at least 8/12 top-3 hits and exceeds K0; report failure or ties honestly. Cosine threshold 0.30 is a fixed uncalibrated display cutoff, not confidence. BM25 requires positive score.

Correctness: unique system-prefixed IDs, edge endpoints, mapping endpoints, unchanged source counts and source hashes. Failure cases: empty query, >500 characters, unrelated input, malformed request, unavailable model. Browser: search, data toggle, method toggle, system filter, source/example panel, keyboard focus, 390px overflow. Human study remains pending; use tests/HUMAN_TASKS.md to collect actual authorized records.
